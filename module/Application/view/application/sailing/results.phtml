<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */
?>
<div class="jumbotron mb-5 secondary-color">
    <div class="container">
        <h1 class="display-4">Sailing Results</h1>
    </div>
</div>

<?php if($this->selected): ?>
    <!-- display selected result -->
    <div class="container-fluid justify-content-center text-center pb-3">
        <?= $this->content ?>
    </div>
<?php endif; ?>

<!-- display all results -->
<?php foreach($this->allResults as $year => $results): ?>
    <div class="container pb-3">
        <h2 class="page-header mb-1 pb-1"><?= $year ?></h2>
        <ul class="list-group list-group-flush">
            <?php foreach($results as $result): ?>
                <a class="list-group-item list-group-item-action d-flex align-items-center justify-content-between" href="<?= $this->url('sailing/results', ['year' => $result['year'], 'result' => $result['title']]) ?>">
                    <?= $result['title'] ?>
                    <i class="fa fa-external-link-square-alt"></i>
                </a>
            <?php endforeach; ?>
        </ul>
    </div>
<?php endforeach; ?>